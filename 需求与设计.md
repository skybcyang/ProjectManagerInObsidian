# Project Manager for Obsidian - 需求与设计

> **项目目标**: 在 Obsidian 中实现一套完整的项目管理系统，支持版本、项目、特性三层结构化管理。

---

## 1. 核心需求

### 1.1 三层数据管理

| 层级 | 存储位置 | 核心字段 | 说明 |
|------|---------|---------|------|
| **版本** | `Versions/*.md` | id, name, status, startDate, endDate, owner, tags | 产品版本迭代 |
| **项目** | `Projects/*.md` | id, name, status, priority, versionId, owner, tags | 必须关联版本 |
| **特性** | `Features/*.md` | id, name, status, priority, versionId, projectId, dueDate, progress, owner, tags | 必须关联项目 |

**数据存储原则**:
- 元数据放在 frontmatter 中
- 详细描述、进度反馈写入正文
- 使用 `[[WikiLink]]` 建立关联

### 1.2 视图与交互

| 功能 | 实现方式 | 状态 |
|------|---------|------|
| 统一视图 | `pm-view` 代码块，支持 6 种模式 | ✅ 已实现 |
| 看板视图 | `mode: kanban` 按状态分栏 | ✅ 已实现 |
| 网格视图 | `mode: grid` 卡片网格 | ✅ 已实现 |
| 级联视图 | `mode: cascade` 版本→项目→特性 | ✅ 已实现 |
| ~~单卡片~~ | ~~`mode: card` 展示单个实体~~ | ❌ 已删除 |
| 面包屑导航 | 层级路径，支持点击穿透 | ✅ 已实现 |
| ICS 导出 | 导出特性截止日期到日历 | ✅ 已实现 |

---

## 2. 设计原则

### 2.1 双层架构

```
UI Layer (Presentation)
├── ViewEngine - 统一视图引擎
├── CardRegistry - 卡片组件注册表
└── Components - 面包屑、选择器等

Core Layer (Data)
├── EntityManager - 统一实体管理
├── VersionStore / ProjectStore / FeatureStore
└── FileSystem - 文件操作封装
```

### 2.2 核心设计原则

1. **文件即真相** - 所有数据存储在 Markdown 中，关闭插件仍可阅读
2. **Obsidian 原生优先** - 优先使用 Obsidian 原生功能，不造轮子
3. **弱引用与容错** - 允许链接断裂，删除文件不导致系统崩溃
4. **渐进式复杂度** - 用户可从简单开始，按需启用高级功能

### 2.3 视图代码块配置

```markdown
```pm-view
mode: kanban          # kanban/grid/cascade/card/timeline/calendar/selector
type: feature         # version/project/feature
groupBy: status       # status/priority/version/project
cols: 3               # 网格列数
filter:
  status: in-progress
  priority: high
sortBy: dueDate
```
```

---

## 3. 数据模型

### 3.1 版本状态

| 状态 | 说明 |
|------|------|
| `planning` | 规划中 |
| `in-progress` | 进行中 |
| `completed` | 已完成 |
| `archived` | 已归档 |

### 3.2 项目/特性状态

| 状态 | 说明 |
|------|------|
| `backlog` | 待规划 |
| `todo` | 待办 |
| `in-progress` | 进行中 |
| `testing` | 测试中 |
| `completed` | 已完成 |
| `archived` | 已归档 |

### 3.3 优先级

| 优先级 | 说明 |
|--------|------|
| `critical` | 紧急 |
| `high` | 高 |
| `medium` | 中 |
| `low` | 低 |

---

## 4. 技术栈

| 层级 | 技术 |
|------|------|
| 语言 | TypeScript |
| 构建 | esbuild |
| 测试 | Jest |
| UI | 原生 DOM + CSS |

---

## 5. 相关文档

- [核心设计原则](./docs/核心设计原则.md)
- [技术架构](./docs/技术架构.md)
- [用户手册](./README.md)
