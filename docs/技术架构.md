# 技术架构文档

> **文档状态**: ✅ 已更新
> **最后更新**: 2026-04-16
> **版本**: v0.8.4

---

## 1. 技术栈

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 语言 | TypeScript 5.3+ | Obsidian 插件开发标准语言 |
| 运行时 | Node.js 20+ | 开发环境 |
| 构建工具 | esbuild 0.20+ | Obsidian 官方推荐 |
| 测试 | Jest 29.7+ | 单元测试 |
| UI 框架 | 原生 DOM + CSS | 减少依赖，直接控制渲染 |

---

## 2. 架构设计

### 2.1 整体架构（三层存储 + 独立需求 + 视图引擎）

```
┌─────────────────────────────────────────────────────────────┐
│                     UI Layer (Presentation)                  │
│  ┌─────────────────────────────────────────────────────┐    │
│  │ main.ts - 插件入口                                   │    │
│  │  - 注册代码块处理器 (pm-view)                       │    │
│  │  - 注册设置面板 (TemplateSettingTab)                │    │
│  │  - 初始化 EntityManager + EntityCache               │    │
│  └─────────────────────────────────────────────────────┘    │
│                          │                                   │
│  ┌───────────────────────┴───────────────────────┐          │
│  │                                               │          │
│  ▼                                               ▼          │
│  ┌─────────────────┐                    ┌─────────────────┐ │
│  │   ViewEngine    │                    │  TemplateService│ │
│  │                 │                    │                 │ │
│  │ - KanbanRenderer│                    │ - renderVersion │ │
│  │ - CascadeRender │                    │ - renderProject │ │
│  │ - TimeViewRender│                    │ - renderRequire │ │
│  │                 │                    │ - renderFeature │ │
│  │                 │                    │                 │ │
│  └────────┬────────┘                    └─────────────────┘ │
│           │                                                 │
│  ┌────────┴──────────────────────────────────────┐          │
│  │                                               │          │
│  ▼                                               ▼          │
│  ┌─────────────────┐                    ┌─────────────────┐ │
│  │ ToolbarController│                   │   EntityCard    │ │
│  │  (工具栏/筛选)   │                    │  (统一卡片组件)  │ │
│  └─────────────────┘                    └─────────────────┘ │
└─────────────────────────────────────────────────────────────┘
                              │
                              │ 依赖注入
                              ▼
┌─────────────────────────────────────────────────────────────┐
│                     Core Layer (Data & File)                 │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              EntityManager (统一入口)                 │    │
│  │         ┌──────────┬──────────┬──────────┐                     │    │
│  │         │ Version  │ Project  │ Feature  │                     │    │
│  │         │  Store   │  Store   │  Store   │                     │    │
│  │         └────┬─────┴────┬─────┴────┬─────┘                     │    │
│  │              └──────────┴──────────┘                            │    │
│  │                                                                 │    │
│  │         RequirementStore（独立管理）                              │    │
│  │                   │                                             │    │
│  │                        │                             │    │
│  │              ┌─────────┴─────────┐                   │    │
│  │              │   EntityCache     │                   │    │
│  │              │  (内存缓存层)      │                   │    │
│  │              └─────────┬─────────┘                   │    │
│  │                        │                             │    │
│  │              ┌─────────┴─────────┐                   │    │
│  │              │    FileSystem     │                   │    │
│  │              │   (文件操作层)     │                   │    │
│  │              └─────────┬─────────┘                   │    │
│  │                        │                             │    │
│  │              ┌─────────┴─────────┐                   │    │
│  │              │   Obsidian API    │                   │    │
│  │              │  (Vault/Metadata) │                   │    │
│  │              └───────────────────┘                   │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 三层存储架构 + 独立需求缓存

```
┌─────────────────────────────────────────────────────────────┐
│ Layer 1: 内存缓存 (EntityCache)                              │
│ - Map<string, Version> versionCache                         │
│ - Map<string, Project> projectCache                         │
│ - Map<string, Requirement> requirementCache                 │
│ - Map<string, Feature> featureCache                         │
│ - 文件监听自动更新 (create/modify/delete/rename)            │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│ Layer 2: Obsidian 元数据缓存 (metadataCache)                 │
│ - 自动解析 frontmatter                                      │
│ - 监听文件变更事件                                          │
└─────────────────────────────────────────────────────────────┘
                              ↕
┌─────────────────────────────────────────────────────────────┐
│ Layer 3: 文件系统 (Vault)                                    │
│ ProjectManager/                                             │
│ ├── 总览.md                                                 │
│ ├── Versions/                                               │
│ │   └── {版本名}.md                                         │
│ ├── Projects/                                               │
│ │   └── {项目名}.md                                         │
│ ├── Requirements/                                           │
│ │   └── {版本名-(项目名-)需求名}.md                         │
│ └── Features/                                               │
│     └── {特性名}.md                                         │
└─────────────────────────────────────────────────────────────┘
```

### 2.3 目录结构

```
src/
├── core/                          # 核心数据层
│   ├── cache/                     # 内存缓存
│   │   ├── EntityCache.ts         # 实体缓存管理
│   │   └── index.ts
│   ├── stores/                    # 实体存储
│   │   ├── BaseStore.ts           # 存储基类
│   │   ├── VersionStore.ts        # 版本存储
│   │   ├── ProjectStore.ts        # 项目存储
│   │   ├── RequirementStore.ts    # 需求存储
│   │   ├── FeatureStore.ts        # 特性存储
│   │   └── index.ts
│   ├── filesystem/                # 文件系统
│   │   ├── FileSystem.ts          # 文件操作封装
│   │   └── index.ts
│   ├── EntityManager.ts           # 统一实体管理入口
│   └── index.ts
├── view-engine/                   # 视图引擎 (统一视图系统)
│   ├── renderers/                 # 视图渲染器
│   │   ├── BaseRenderer.ts        # 渲染器基类
│   │   ├── KanbanRenderer.ts      # 看板视图
│   │   ├── CascadeRenderer.ts     # 级联视图
│   │   ├── TimeViewRenderer.ts    # 时间视图（甘特图）
│   │   └── index.ts
│   ├── components/                # 视图组件
│   │   ├── EntityCard.ts          # 统一卡片组件
│   │   ├── EntityTreeSelector.ts  # 树形层级选择器
│   │   ├── DropdownMenu.ts        # 下拉菜单组件
│   │   ├── StatusPicker.ts        # 状态选择器
│   │   ├── ProgressPicker.ts      # 进度选择器
│   │   └── index.ts
│   ├── cells/                     # 行内编辑单元格
│   │   ├── BaseCell.ts
│   │   ├── TextCell.ts
│   │   ├── SelectCell.ts
│   │   ├── DateCell.ts
│   │   ├── ProgressCell.ts
│   │   ├── MultiSelectCell.ts
│   │   └── index.ts
│   ├── services/                  # 视图服务
│   │   ├── DataService.ts         # 数据加载服务
│   │   ├── ActionService.ts       # 操作服务
│   │   ├── EntityStrategy.ts      # 实体策略模式
│   │   └── index.ts
│   ├── controllers/               # 视图控制器
│   │   ├── ToolbarController.ts   # 工具栏控制器
│   │   ├── SortMenuController.ts  # 排序菜单控制器
│   │   ├── PropertyPanelController.ts  # 属性面板控制器
│   │   └── index.ts
│   ├── types.ts                   # 视图类型定义
│   ├── design-tokens.ts           # 设计令牌
│   ├── RendererRegistry.ts        # 渲染器注册表
│   ├── ViewEngine.ts              # 视图引擎主类
│   └── index.ts
├── services/                      # 全局服务
│   ├── TemplateService.ts         # 模板渲染服务
│   ├── DashboardService.ts        # 仪表盘服务
│   ├── VersionService.ts          # 版本服务
│   ├── ProjectService.ts          # 项目服务
│   ├── FeatureService.ts          # 特性服务
│   ├── DataviewService.ts         # Dataview 查询服务
│   ├── InitService.ts             # 初始化服务
│   ├── CodeBlockConfigService.ts  # 代码块配置服务
│   ├── ChangeLogService.ts        # 变更日志服务
│   ├── ReportService.ts           # 报告服务
│   └── index.ts
├── templates/                     # 模板系统
│   ├── defaults.ts                # 默认模板定义
│   └── index.ts
├── settings/                      # 设置面板
│   ├── TemplateSettingTab.ts      # 模板设置
│   └── index.ts
├── modals/                        # 模态框
│   ├── CreateVersionModal.ts
│   ├── CreateProjectModal.ts
│   ├── CreateRequirementModal.ts
│   ├── CreateFeatureModal.ts
│   ├── QuickCreateModal.ts
│   ├── ExportICSModal.ts
│   └── index.ts
├── ui/                            # UI 组件
│   ├── cards/                     # 卡片组件
│   │   ├── CardRegistry.ts
│   │   ├── VersionCard.ts
│   │   ├── ProjectCard.ts
│   │   ├── FeatureCard.ts
│   │   └── index.ts
│   └── components/                # 通用组件
│       ├── Breadcrumb.ts
│       ├── DatePicker.ts
│       ├── ProgressInput.ts
│       └── index.ts
├── types/                         # 类型定义
│   ├── version.ts
│   ├── project.ts
│   ├── feature.ts
│   ├── template.ts
│   ├── changelog.ts               # 变更日志类型
│   └── index.ts
├── constants/                     # 常量定义
│   ├── statuses.ts                # 状态定义
│   ├── priorities.ts              # 优先级定义
│   └── index.ts
├── utils/                         # 工具函数
│   ├── icsGenerator.ts
│   ├── idGenerator.ts
│   ├── fileManager.ts
│   ├── validator.ts
│   ├── configValidator.ts         # 配置验证
│   ├── errorHandler.ts            # 错误处理
│   └── index.ts
├── stories/                       # Storybook 组件测试
│   ├── *.stories.ts               # 各组件 story 定义
│   └── Introduction.mdx           # Storybook 介绍
└── main.ts                        # 插件入口
```

```
tests/                             # 测试目录（Jest）
├── core/                          # 核心层测试
├── view-engine/                   # 视图引擎测试
└── utils/                         # 工具函数测试
```

---

## 3. 核心机制

### 3.1 文件命名规则

| 实体 | 文件名格式 | 示例 |
|------|-----------|------|
| 版本 | `{版本名}.md` | `V1.0 Initial Release.md` |
| 项目 | `{项目名}.md` | `用户系统.md` |
| 需求 | `{版本名-(项目名-)需求名}.md` | `V1.0-用户系统-登录优化.md` / `V1.0-注册流程优化.md` |
| 特性 | `{特性名}.md` | `登录功能.md` |

> **注意**: v0.6.0 起不再在文件名中包含层级关系，改为通过 frontmatter 中的 versionId/projectId 关联

### 3.2 缓存同步机制

```
用户删除文件夹
    ↓
Obsidian 触发 'delete' 事件
    ↓
EntityCache.handleFileDelete(file)
    ↓
通过文件名匹配查找缓存项
    ↓
从对应 Map 中 delete(id)
    ↓
缓存与文件系统保持一致
```

### 3.3 视图模式切换与配置状态管理

```
用户点击视图切换按钮
    ↓
ToolbarController.onViewModeChange(newMode)
    ↓
ViewEngine 从 currentConfigs 读取最新配置
    ↓
合并新 mode，更新 currentConfigs
    ↓
保存新配置到代码块
    ↓
重新创建 Renderer
    ↓
调用新 Renderer.render()
    ↓
视图刷新
```

**currentConfigs 机制**：
- `ViewEngine` 使用 `Map<string, ViewConfig>` 维护每个代码块的实时配置
- 所有变更回调（筛选、排序、属性、视图切换）统一读写 `currentConfigs`
- 彻底消除 ToolbarController 回调闭包陷阱，避免切换视图后筛选条件 revert

---

## 4. 视图系统

### 4.1 支持的视图模式 (v0.8.1)

| 视图 | 模式值 | 渲染器 | 主要用途 |
|------|--------|--------|----------|
| 看板视图 | `kanban` | KanbanRenderer | 类 Trello 看板，按状态/优先级分组 |
| 需求看板 | `kanban` + `entityType: requirement` | KanbanRenderer | 需求按状态/优先级分组展示 |
| 级联视图 | `cascade` | CascadeRenderer | 版本→项目→特性层级，展示进展/风险详情 |
| 时间视图 | `timeview` | TimeViewRenderer | 甘特图，按负责人/项目分组 |

### 4.2 统一卡片组件 (EntityCard)

**可配置字段：**
- `showPriority`: 优先级标记条
- `showStatus`: 状态徽章
- `showOwner`: 负责人
- `showStartDate`: 开始日期
- `showDueDate`: 截止日期
- `showEstimatedDays`: 预估人天
- `showActualDays`: 实际人天
- `showProgress`: 进度条
- `showRisk`: 风险徽章
- `showLatestProgress`: 最新进展
- `showTags`: 标签
- `smallTitle`: 小标题模式

### 4.3 级联视图卡片字段配置

```yaml
mode: cascade
entityType: feature
cardFields:
  required: [name, priority]
  optional: [status, owner, startDate, endDate, estimatedDays, actualDays, progress, risk, latestProgress, tags]
```

---

## 5. 模板系统

### 5.1 模板类型

| 类型 | 用途 | 文件 |
|------|------|------|
| overview | 项目管理总览入口 | 总览模板 |
| version | 版本详情页 | 版本模板 |
| project | 项目详情页 | 项目模板 |
| requirement | 需求详情页 | 需求模板 |
| feature | 特性详情页 | 特性模板 |

### 5.2 模板变量

支持嵌套属性访问（v0.6.0+）：

```markdown
# 📦 {{name}}
> **状态**: {{status}} | **ID**: {{id}}
{{#if owner}}> **负责人**: @{{owner}}{{/if}}
```

---

## 6. 性能优化

| 场景 | 策略 |
|------|------|
| 大量文件加载 | 内存缓存 + 懒加载 |
| 频繁文件变更 | Obsidian 事件监听，增量更新 |
| 关系查询 | 通过 ID 弱引用，实时查找 |
| 视图渲染 | 原生 DOM，无框架开销 |
| 筛选过滤 | 内存中过滤，不重复读取文件 |

---

## 7. 扩展指南

### 7.1 添加新视图模式

1. 在 `view-engine/renderers/` 创建新渲染器类，继承 `BaseRenderer`
2. 实现 `render(container: HTMLElement)` 方法
3. 在文件末尾调用 `RendererRegistry.register('modeName', NewRenderer)`
4. 在 `ViewMode` 类型中添加新模式
5. 在 `VIEW_MODE_LABELS` 中添加显示名称

### 7.2 自定义模板

1. 在设置面板中启用自定义模板
2. 编辑模板文件
3. 使用模板变量：`{{name}}`, `{{status}}`, `{{#if}}...{{/if}}`
4. 支持嵌套属性：`{{project.name}}`

### 7.3 使用配置验证

```typescript
import { ConfigValidator } from './utils';

const validation = ConfigValidator.validate(parsedConfig);
if (!validation.valid) {
  validation.errors.forEach(err => console.warn(err));
}
```

### 7.4 错误处理

```typescript
import { ErrorHandler } from './utils';

try {
  await operation();
  ErrorHandler.handleSuccess('操作成功');
} catch (error) {
  ErrorHandler.handle(error, '上下文信息', { category: 'user' });
}
```

---

## 8. Q&A

| 日期 | 问题 | 决策 |
|-----|------|------|
| 2026-04-02 | 架构设计？ | Core + UI 双层架构 |
| 2026-04-06 | 如何实现统一视图？ | ViewEngine + Renderers |
| 2026-04-07 | 文件命名规则？ | 使用 ID 关联，文件名不携带层级 |
| 2026-04-08 | 如何优化性能？ | EntityCache + 懒加载策略 |

---

*文档结束*
